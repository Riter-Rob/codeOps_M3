My Restaurant

A beginner React project that practices JSX, components, props, and rendering an array with `map`.

## How to run

Install the packages:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Open the local URL shown in the terminal.
